import styled from "styled-components";

export const Request = styled.form`
    width: 50%;
    
    p {
        span {
            color: var(--negative);
            font-size: 0.7rem;
        }
    }
`